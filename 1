directoryentry * b =getrootdirectory();

while (parspath[i] != NULL){
    directoryEntry *b = getdirectory(b, parspath[i] );
    i ++;
}

makedirect(b,childname);












Path *path = parsePath("/di/d2/d3/2");

DirectoryEntry *dir = getRootDirectory();
int i =0;
while(path[i] != NULL){
    DirectoryEntry* dir = getDirectory(dir, path->tokens[i]);
}